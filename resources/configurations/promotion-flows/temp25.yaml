apiVersion: codefresh.io/v1beta1
kind: PromotionFlow
metadata:
  name: temp25
  annotations:
    description: ""
spec:
  triggerEnvironment: test
  steps:
    - environment: prod
      dependsOn:
        - test
      policy:
        action: commit
